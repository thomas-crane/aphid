/**
 * Configuration values for the @see AphidClient class.
 */
export interface AphidClientOptions {
  /**
   * The prefix which will trigger commands for this Aphid bot.
   */
  prefix: string;
  /**
   * The author of this Aphid bot.
   */
  author: string;
}
