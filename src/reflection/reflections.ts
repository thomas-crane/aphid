/**
 * Strings which can be used with `Reflect.getMetadata` to retrieve
 * various bits of information.
 */
export enum Reflections {
  /**
   * The key used to reflect parameter types of a function.
   */
  ParamTypes = 'design:paramtypes',
}
